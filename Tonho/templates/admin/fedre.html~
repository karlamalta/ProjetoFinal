{% extends "admin/base_site.html" %}
{% load i18n admin_static bootstrapped_goodies_tags %}
{% load mptt_tags %}

{% block extrastyle %}{{ block.super }}<link rel="stylesheet" type="text/css" href="{% static "admin/css/dashboard.css" %}" />{% endblock %}

{% block coltype %}colMS{% endblock %}

{% block bodyclass %}dashboard{% endblock %}

{% block breadcrumbs %}
<ul class="breadcrumb">
<li>{% trans 'FeDRE' %}</li>
</ul>
{% endblock %}

{% block navbar %}
{% endblock %}

{% block content %}

<h3>Branch {{index}}</h3>

{% recursetree features.all %}
    <li>
        <label for="id_{{ name }}_{{ node.pk }}">{{ node.name }}</label>
        {% if not node.is_leaf_node %}
            <ul>
                {{ children }}
            </ul>
        {% endif %}
    </li>
{% endrecursetree %}

<h5>Features that must have use case:</h5>

{% for f in must_have_use_case %}
    <li>
        {{ f.name }}
    </li>
{% endfor %}

<p>Features that may have use case:</p>

{% for f in may_have_use_case %}
    <li>
        {{ f.name }}
    </li>
{% endfor %}

<p>Features that should not have use case:</p>

{% for f in should_not_have_use_case %}
    <li>
        {{ f.name }}
    </li>
{% endfor %}

<form method="POST">{% csrf_token %}
    <input type="hidden" name="index" value= {{ index }}>
    <button type="submit" class="save btn btn-default">Branch Done</button>
</form>

{% endblock %}

{% block sidebar %}
{% endblock %}
